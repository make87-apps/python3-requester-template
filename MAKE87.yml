build_kit:
  name: python3
  version: latest
requester_endpoints:
  - name: EXAMPLE_ENDPOINT
    requester_message_type: make87_messages.text.text_plain.PlainText
    provider_message_type: make87_messages.text.text_plain.PlainText
config:
  values:
    - name: ROOT_ENTITY
      description: "The root entity path used to build the data tree"
      default_value: "/"
      required: false
      secret: false